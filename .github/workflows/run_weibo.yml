name: Run Weibo Script

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2
      
    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9' # 你想要使用的 Python 版本
        
    - name: Install Dependencies
      run: |
        pip install -r requirements.txt
        pip install lxml_html_clean

    - name: Run Python Script
      env:
        Telegram_Bot_Token: ${{ secrets.self.TELEGRAM_BOT_TOKEN }}
        Telegram_Chat_ID: ${{ secrets.self.TELEGRAM_CHAT_ID }}
        Weibo_ID: ${{ secrets.self.WEIBO_ID }}
        Proxy: ${{ secrets.self.PROXY }}
      run: python weibo.py
